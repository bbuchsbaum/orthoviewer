% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_ortho.R
\name{layer}
\alias{layer}
\title{Add an overlay layer to the viewer}
\usage{
layer(
  builder,
  vol,
  colormap = "hot",
  thresh = c(-2, 2),
  range = NULL,
  opacity = 0.8,
  id = NULL
)
}
\arguments{
\item{builder}{An \code{ortho_builder} object from \code{view_ortho()}.}

\item{vol}{The overlay volume - a 3D array or neuroim2 NeuroVol object.}

\item{colormap}{Character: colormap for this layer (default "hot").
Options include "viridis", "plasma", "hot", "cool", "RdBu", etc.}

\item{thresh}{Numeric vector of length 2: threshold range. Values between
the low and high thresholds are made transparent. Default c(-2, 2) works
well for z-score maps.}

\item{range}{Numeric vector of length 2: display window range. If NULL,
the volume's data range is used.}

\item{opacity}{Numeric: layer opacity from 0 (transparent) to 1 (opaque).
Default 0.8.}

\item{id}{Character: unique identifier for this layer. If NULL, an
automatic id is generated.}
}
\value{
The modified \code{ortho_builder} object (for chaining).
}
\description{
Adds a volume as an overlay layer with its own colormap, threshold,
opacity, and display range settings.
}
\examples{
\dontrun{
view_ortho(brain) |>
  layer(stat_map, colormap = "hot", thresh = c(-3, 3)) |>
  layer(roi_mask, colormap = "cool", opacity = 0.5)
}
}
