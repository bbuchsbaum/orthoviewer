% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ortho_events.R
\name{ortho_on_click}
\alias{ortho_on_click}
\title{React to viewer click events}
\usage{
ortho_on_click(viewer_id, handler, session = NULL, ...)
}
\arguments{
\item{viewer_id}{Character: the outputId of the ortho_viewer widget.}

\item{handler}{A function that takes a single argument (the ortho_event
object) and performs some action.}

\item{session}{The Shiny session object. If NULL, uses the current
reactive domain.}

\item{...}{Additional arguments passed to \code{observeEvent} (e.g.,
\code{ignoreInit}, \code{once}, \code{priority}).}
}
\value{
An observer reference (invisibly), as returned by \code{observeEvent}.
}
\description{
A convenience wrapper around \code{observeEvent} that handles the Shiny
input naming and event normalization automatically. For more control,
use \code{\link{ortho_clicks}} directly.
}
\examples{
\dontrun{
server <- function(input, output, session) {
  output$viewer <- renderOrtho_viewer({ ortho_viewer(brain) })

  # Simple click handler
  ortho_on_click("viewer", function(click) {
    world <- ortho_world(click)
    showNotification(paste("Clicked:", round(world[1], 1), round(world[2], 1), round(world[3], 1)))
  }, session)
}
}
}
