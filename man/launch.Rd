% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_ortho.R
\name{launch}
\alias{launch}
\title{Launch the viewer}
\usage{
launch(builder, run = TRUE)
}
\arguments{
\item{builder}{An \code{ortho_builder} object.}

\item{run}{Logical: if TRUE (default), launches the app immediately.
If FALSE, returns the Shiny app object without launching.}
}
\value{
If \code{run = TRUE}, returns the app invisibly after it closes.
If \code{run = FALSE}, returns the Shiny app object.
}
\description{
Builds and runs the Shiny app from the builder specification.
}
\examples{
\dontrun{
# Launch immediately
view_ortho(brain) |> launch()

# Get app object without launching
app <- view_ortho(brain) |> launch(run = FALSE)
}
}
