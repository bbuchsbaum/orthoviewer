# Set viewer title

Set viewer title

## Usage

``` r
set_title(builder, title)
```

## Arguments

- builder:

  An `ortho_builder` object.

- title:

  Character: the window/page title.

## Value

The modified `ortho_builder` object (for chaining).
