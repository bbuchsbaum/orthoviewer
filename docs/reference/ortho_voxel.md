# Get voxel indices from an ortho_viewer event

Get voxel indices from an ortho_viewer event

## Usage

``` r
ortho_voxel(event)
```

## Arguments

- event:

  An ortho_event object or the raw input value.

## Value

Integer vector of length 3 (i, j, k) as voxel indices, or NULL if not
available.
