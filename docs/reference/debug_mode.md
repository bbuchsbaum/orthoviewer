# Enable debug mode

When enabled, verbose logging is printed to the browser console.

## Usage

``` r
debug_mode(builder, enable = TRUE)
```

## Arguments

- builder:

  An `ortho_builder` object.

- enable:

  Logical: whether to enable debug mode. Default TRUE.

## Value

The modified `ortho_builder` object (for chaining).
