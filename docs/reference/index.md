# Package index

## All functions

- [`controls()`](controls.md) : Toggle the control panel

- [`debug_mode()`](debug_mode.md) : Enable debug mode

- [`format_world_coord()`](format_world_coord.md) : Format world
  coordinates in anatomical notation

- [`launch()`](launch.md) : Launch the viewer

- [`layer()`](layer.md) : Add an overlay layer to the viewer

- [`ortho_event()`](ortho_event.md) : Get click event data from
  ortho_viewer

- [`ortho_events`](ortho_events.md) : Ortho Viewer Event Handling

- [`ortho_intensity()`](ortho_intensity.md) : Get intensity values from
  an ortho_viewer event

- [`ortho_modifier()`](ortho_modifier.md) : Check if a modifier key was
  held during an event

- [`ortho_proxy()`](ortho_proxy.md) : Proxy object for controlling an
  orthogonal viewer from Shiny

- [`ortho_viewer()`](ortho_viewer.md) : Orthogonal neuroimaging viewer
  (htmlwidget shell)

- [`ortho_viewerOutput()`](ortho_viewerOutput.md) :

  Shiny output for `ortho_viewer`

- [`ortho_voxel()`](ortho_voxel.md) : Get voxel indices from an
  ortho_viewer event

- [`ortho_world()`](ortho_world.md) : Get world coordinates from an
  ortho_viewer event

- [`orthoviewer_click_example()`](orthoviewer_click_example.md) : Run an
  interactive orthoviewer example with click handling

- [`orthoviewer_example_app()`](orthoviewer_example_app.md) : Run a
  minimal orthoviewer Shiny example

- [`orthoviewer_navigation_example()`](orthoviewer_navigation_example.md)
  : Run an interactive orthoviewer example with navigation controls

- [`print(`*`<ortho_event>`*`)`](print.ortho_event.md) : Print method
  for ortho_event objects

- [`renderOrtho_viewer()`](renderOrtho_viewer.md) :

  Shiny render function for `ortho_viewer`

- [`set_title()`](set_title.md) : Set viewer title

- [`status_bar()`](status_bar.md) : Toggle the status bar

- [`view_ortho()`](view_ortho.md) : Create an orthogonal brain viewer
