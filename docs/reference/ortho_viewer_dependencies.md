# htmlwidgets dependencies for the ortho_viewer widget

Returns NULL since all dependencies are now specified in the
ortho_viewer.yaml file (neuroimjs bundle, CSS, etc.).

## Usage

``` r
ortho_viewer_dependencies()
```
