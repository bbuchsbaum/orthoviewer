# Shiny render function for `ortho_viewer`

Shiny render function for `ortho_viewer`

## Usage

``` r
renderOrtho_viewer(expr, env = parent.frame(), quoted = FALSE)
```

## Arguments

- expr:

  An expression that returns an `ortho_viewer` widget.

- env, quoted:

  Standard `htmlwidgets` arguments.
